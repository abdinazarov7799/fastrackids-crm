import { CreateCallRequestDto } from './create-call-request.dto';

describe('CreateCallRequestDto', () => {
  it('should be defined', () => {
    expect(new CreateCallRequestDto()).toBeDefined();
  });
});
