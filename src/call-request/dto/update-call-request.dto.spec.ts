import { UpdateCallRequestDto } from './update-call-request.dto';

describe('UpdateCallRequestDto', () => {
  it('should be defined', () => {
    expect(new UpdateCallRequestDto()).toBeDefined();
  });
});
